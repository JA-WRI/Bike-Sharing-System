name: Full Stack CI

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  backend:
    name: Backend - Spring Boot
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'

      - name: Make Maven Wrapper executable
        working-directory: Backend/veloMTL
        run: chmod +x mvnw

      - name: Run backend tests
        working-directory: Backend/veloMTL
        run: ./mvnw test



  frontend:
    name: Frontend - React
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        working-directory: Frontend/VeloMTL
        run: npm ci

      - name: Build frontend
        working-directory: Frontend/VeloMTL
        run: npm run build
